[package]
name = "mandelbrot-viewer"
version = "0.1.0"
edition = "2021"

[dependencies]
mandelbrot-lib = { path = "../mandelbrot-lib" }
minifb = "0.27.0"
image = "0.25.0"           # needed because lib returns RgbImage
