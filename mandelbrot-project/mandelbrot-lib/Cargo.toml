[package]
name = "mandelbrot-lib"
version = "0.1.0"
edition = "2024"

[dependencies]
image = { version = "0.25", features = ["rayon"] }
rayon = "1.10"  # For parallelism
num_cpus = "1.16.0"  # For auto-detecting threads

